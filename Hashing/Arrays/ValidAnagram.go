package main

// import "fmt"

// "strings"

// func isAnagram(s string, t string) bool {

// 	sHash := make(map[string]bool)
// 	tsHash := make(map[string]bool)

// 	for i := 0; i < (len(s + t)); i++ {
// 		if sHash[s+t] == tsHash[s+t] {
// 			return true
// 		}

// 	}

// 	return false
// }
